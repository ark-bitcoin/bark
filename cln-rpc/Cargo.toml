[package]
name = "bark-cln-rpc"
version = "0.0.0"
edition = "2021"

[lib]
name = "cln_rpc"

[dependencies]
ark-lib = { path = "../lib" }
bitcoin.workspace = true
tonic.workspace = true
prost.workspace = true

[build-dependencies]
tonic-build.workspace= true
